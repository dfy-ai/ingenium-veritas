{
  "name": "ingenium-veritas",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/client/index.tsx --bundle --splitting --format=esm --platform=browser --outdir=public/dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}